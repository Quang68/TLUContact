<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:padding="16dp"
    tools:context=".Chitiet.SinhVienChiTietActivity"
    android:gravity="center_horizontal"
    android:fitsSystemWindows="true">

    <!-- Avatar -->
    <ImageView
        android:id="@+id/imv_AvatarSV"
        android:layout_width="120dp"
        android:layout_height="120dp"
        android:layout_marginTop="32dp"
        android:layout_marginBottom="16dp"
        android:scaleType="centerCrop"
        android:src="@drawable/avata" />

    <!-- Tên sinh viên -->
    <TextView
        android:id="@+id/txt_nameSV"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Tên sinh viên"
        android:textColor="#000000"
        android:textSize="20sp"
        android:textStyle="bold"
        android:gravity="center" />

    <!-- Mã sinh viên -->
    <TextView
        android:id="@+id/txt_maSV"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Mã sinh viên"
        android:textColor="#757575"
        android:textSize="16sp"
        android:gravity="center" />

    <!-- Số điện thoại -->
    <TextView
        android:id="@+id/txt_sdtSV"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Số điện thoại"
        android:textColor="#757575"
        android:textSize="16sp"
        android:gravity="center" />

    <!-- Email -->
    <TextView
        android:id="@+id/txt_emailSV"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Email"
        android:textColor="#757575"
        android:textSize="16sp"
        android:gravity="center" />

    <!-- Địa chỉ -->
    <TextView
        android:id="@+id/txt_diachiSV"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Địa chỉ"
        android:textColor="#757575"
        android:textSize="16sp"
        android:gravity="center" />

    <!-- Lớp -->
    <TextView
        android:id="@+id/txt_lopSV"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Lớp"
        android:textColor="#757575"
        android:textSize="16sp"
        android:gravity="center" />

    <!-- Các nút chức năng -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center"
        android:layout_marginTop="32dp">

        <!-- Button Phone -->
        <androidx.cardview.widget.CardView
            android:layout_width="60dp"
            android:layout_height="60dp"
            android:layout_margin="8dp"
            app:cardCornerRadius="30dp"
            app:cardElevation="4dp">

            <ImageButton
                android:id="@+id/btnPhoneSV"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:background="?attr/selectableItemBackgroundBorderless"
                android:contentDescription="Phone"
                app:srcCompat="@drawable/phone" />
        </androidx.cardview.widget.CardView>

        <!-- Button Email -->
        <androidx.cardview.widget.CardView
            android:layout_width="60dp"
            android:layout_height="60dp"
            android:layout_margin="8dp"
            app:cardCornerRadius="30dp"
            app:cardElevation="4dp">

            <ImageButton
                android:id="@+id/btnChatSV"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:background="?attr/selectableItemBackgroundBorderless"
                android:contentDescription="Email"
                app:srcCompat="@drawable/chat" />
        </androidx.cardview.widget.CardView>

        <!-- Button Email -->
        <androidx.cardview.widget.CardView
            android:layout_width="60dp"
            android:layout_height="60dp"
            android:layout_margin="8dp"
            app:cardCornerRadius="30dp"
            app:cardElevation="4dp">

            <ImageButton
                android:id="@+id/btnEmailSV"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:background="?attr/selectableItemBackgroundBorderless"
                android:contentDescription="Email"
                app:srcCompat="@drawable/email" />
        </androidx.cardview.widget.CardView>
    </LinearLayout>

</LinearLayout>
